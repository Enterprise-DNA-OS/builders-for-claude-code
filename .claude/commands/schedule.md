---
description: This week on site: what is planned, what is in progress, what is late. Every late task pushes the claim behind it.
---

1. Run `npm run builders -- schedule` (the next 7 days; `--all` for everything planned, `--job=` for one job).
2. Late tasks are the headline: name the task, the days late, and the subbie or trade, then the knock-on (which claim it pushes).
3. Done: `task done <job> "<task>"`. New: `task add <job> "<task>" --starts= --ends= [--trade= --subbie=]`.
4. If the operator wants a page for the wall or the Monday meeting: `npm run view` renders the board in the business's brand.
